node_modules/
dist/
src-tauri/target/
src-tauri/gen/schemas/
*.log
.DS_Store
Thumbs.db
*.bak-*

# Phase 0 helper installer (binary); скрипт .nsi в git
scripts/*.exe

# Сырые прогоны бенча, не артефакт релиза
docs/phase0-bench*.jsonl
docs/phase0-bench*.csv
docs/phase0-bench.err.txt

# Updater signing keys — приватный ключ НИКОГДА не коммитить
.tauri/

# Одноразовые помощники для поднятия VM под Linux-порт: держим на диске, но из репозитория
# исключаем. Если что-то из этого станет постоянным — вынести и закоммитить осознанно.
scripts/vm-*
scripts/vm_*
scripts/write_*.py
scripts/make_vm_seed_iso.py
scripts/cloud-init/
