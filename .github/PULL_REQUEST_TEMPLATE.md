<!-- Одна правка - одна тема. Перестановку кода и смысл разносите по разным коммитам. -->

**Что сделали**
Одной строкой, как в сообщении коммита.

**Зачем**
Что было не так до этой правки.

**Как проверяли**
- [ ] `npm run typecheck`, `npm run lint`, `npm test`
- [ ] `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --lib`
- [ ] стенд (`scripts/ssh-stand/up.sh` + `cargo test -- --ignored --test-threads=1`), если
      правка касается SSH, SFTP, SCP, баз или рабочего стола
- [ ] проверено в запущенном приложении, если менялся интерфейс

**Что может сломаться**
Соседние места, до которых дотянулась правка.
